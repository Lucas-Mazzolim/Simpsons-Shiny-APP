<<<<<<< HEAD
comparacaoCategorias_server <- function(id, dados) {
  moduleServer(id, function(input, output, session) {
    # Lógica de renderização do aplicativo aqui
  })
}

comparacaoEpisodios_server <- function(id, dados) {
  moduleServer(id, function(input, output, session) {
    # Lógica de renderização do aplicativo aqui
  })
}

evolucaoTemporal_server <- function(id, dados) {
  moduleServer(id, function(input, output, session) {
    # Lógica de renderização do aplicativo aqui
  })
}

panoramaGeral_server <- function(id, dados) {
  moduleServer(id, function(input, output, session) {
    # Lógica de renderização do aplicativo aqui
  })
}

visualizacaoTextual_server <- function(id, dados) {
  moduleServer(id, function(input, output, session) {
    # Lógica de renderização do aplicativo aqui
  })
}

principaisCorrelacoes_server <- function(id, dados) {
  moduleServer(id, function(input, output, session) {
    # Lógica de renderização do aplicativo aqui
  })
}

server = function(input, output, session) {
  # Junção dos servidores para formar um todo coeso
}

=======
library(shiny)
library(ggplot2)
library(bslib)

server = function(input, output) {
  #gerar o histograma, exemplo:
  
  
}


# Roda o app
shinyApp(ui, server)
>>>>>>> 500b88301f30e808b3a3cb553bc667009240a0e2
